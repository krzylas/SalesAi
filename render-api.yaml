services:
  - type: web
    name: sales-gym-api
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DEEPGRAM_API_KEY
        sync: false
